import * as types from './actionConstants';

export const closeNotifAction = {
	type: types.CLOSE_NOTIF
};

export const composeBaseAction = {
	type: types.COMPOSE_PROFILE_BASE,
};

export const discardBaseAction = {
	type: types.DISCARD_PROFILE_BASE,
};

export const composeAboutAction = {
	type: types.COMPOSE_PROFILE_ABOUT,
};

export const discardAboutAction = {
	type: types.DISCARD_PROFILE_ABOUT,
};

export const composeInterestAction = {
	type: types.COMPOSE_PROFILE_INTEREST,
};

export const discardInterestAction = {
	type: types.DISCARD_PROFILE_INTEREST,
};